import 'package:flutter/material.dart';

class AgregarTabTrab extends StatelessWidget {
  const AgregarTabTrab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
import 'package:flutter/material.dart';

class AgendarTabTrab extends StatelessWidget {
  const AgendarTabTrab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}